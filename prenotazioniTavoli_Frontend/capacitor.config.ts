import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'prenotazioniTavoli_Frontend',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
